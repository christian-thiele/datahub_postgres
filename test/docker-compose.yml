services:
  postgres:
    image: postgis/postgis:16-3.4
    environment:
      - POSTGRES_DB=test
      - POSTGRES_USER=test
      - POSTGRES_PASSWORD=secretpassword
    tmpfs:
      - /var/lib/postgresql/data
